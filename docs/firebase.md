# Firebase Setup

Weatherpulse relies on Firebase for true multi-tenant data isolation and real-time syncing. 

## Service Account
Ensure your `FIREBASE_SERVICE_ACCOUNT_KEY` environment variable is correctly set with your base64-encoded or stringified JSON credential.
